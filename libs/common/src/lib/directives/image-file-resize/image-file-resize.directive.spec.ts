import { ImageFileResizeDirective } from './image-file-resize.directive';

describe('ImageFileResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageFileResizeDirective();
    expect(directive).toBeTruthy();
  });
});
